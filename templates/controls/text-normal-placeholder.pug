input(id='{ input.name }',name='{ name || input.name }',type='{ type }',onchange='{ change }',onblur='{ change }',riot-value='{ input.ref.get(input.name) }',autocomplete='{ autoComplete }',class='{invalid: errorMessage, valid: valid}',placeholder='{ placeholder }')
#{ 'yield' }
